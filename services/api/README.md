# API Service

Здесь будет реализован HTTP API для PIFAGOR.

Endpoints:
- POST /upload_photo — загрузка фото  
- GET /get_task — выдача задачи воркеру  
- POST /submit_result — приём JSON-результата от воркера  

