# Worker Service

Сервис обработки задач:
- забирает задачу из очереди  
- вызывает GPT / OCR / обработку карточки  
- сохраняет JSON → либо вызывает API / db_writer  
- перемещает исходное фото в archive_photos/ или error_photos/ при неудаче  

